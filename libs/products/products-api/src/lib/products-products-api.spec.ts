import { productsProductsApi } from './products-products-api';

describe('productsProductsApi', () => {
  it('should work', () => {
    expect(productsProductsApi()).toEqual('products-products-api');
  });
});
